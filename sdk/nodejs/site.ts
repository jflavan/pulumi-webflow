// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages Webflow sites programmatically. This resource allows you to create, configure, and manage Webflow sites through infrastructure code. Create, Read, Update, and Delete operations are fully supported for complete site lifecycle management.
 *
 * **Required scopes:** creating a site (POST /v2/workspaces/{workspace_id}/sites) requires the `workspace:write` scope and an Enterprise workspace; reading a site requires `sites:read`; updating, publishing and deleting a site require `sites:write`.
 *
 * **Import:** `pulumi import webflow:index:Site my-site <siteId>`. The Webflow API does not report the template a site was created from, so `templateName` is empty after import and only affects creation; it is compared only when both the program and the state hold a value.
 */
export class Site extends pulumi.CustomResource {
    /**
     * Get an existing Site resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, opts?: pulumi.CustomResourceOptions): Site {
        return new Site(name, undefined as any, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'webflow:index:Site';

    /**
     * Returns true if the given object is an instance of Site.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Site {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Site.__pulumiType;
    }

    /**
     * List of custom domains attached to the site (read-only in this release). Custom domain management will be available in a future release.
     */
    declare public /*out*/ readonly customDomains: pulumi.Output<string[] | undefined>;
    /**
     * Indicates whether data collection is enabled for this site (read-only). Controlled by your Webflow workspace settings, not per-site.
     */
    declare public /*out*/ readonly dataCollectionEnabled: pulumi.Output<boolean | undefined>;
    /**
     * The type of data collection enabled for the site (read-only). Possible values: 'always' (always collect), 'optOut' (collect by default), 'disabled' (no collection). Controlled by your Webflow workspace settings, not per-site.
     */
    declare public /*out*/ readonly dataCollectionType: pulumi.Output<string | undefined>;
    /**
     * The human-readable name of the site as shown in the Webflow dashboard. Required - must be a non-empty string. Examples: 'My Marketing Site', 'Company Blog', 'Product Landing Page'. This is the name users will see when managing the site.
     */
    declare public readonly displayName: pulumi.Output<string>;
    /**
     * The RFC3339 timestamp of the last time the site was published (read-only). Automatically set by Webflow when changes are published to production.
     */
    declare public /*out*/ readonly lastPublished: pulumi.Output<string | undefined>;
    /**
     * The RFC3339 timestamp of the last time the site configuration was updated (read-only). Automatically set by Webflow when any site changes occur.
     */
    declare public /*out*/ readonly lastUpdated: pulumi.Output<string | undefined>;
    /**
     * The folder ID where the site will be organized in the Webflow dashboard (24-character lowercase hexadecimal string). Optional - the site will be placed at the workspace root if not specified. Removing this property from your program moves the site back to the workspace root. This is useful for organizing multiple sites into logical groups within your workspace.
     */
    declare public readonly parentFolderId: pulumi.Output<string | undefined>;
    /**
     * The URL to a preview image of the site (read-only). Automatically generated by Webflow for site thumbnails in the dashboard.
     */
    declare public /*out*/ readonly previewUrl: pulumi.Output<string | undefined>;
    /**
     * Automatically publish the site after creation or updates. When set to true, the provider calls the Webflow publish endpoint after successfully creating or updating the site, using `publishToWebflowSubdomain`, `publishCustomDomains` and `publishPageId` to build the publish request. If neither `publishToWebflowSubdomain` nor `publishCustomDomains` is set, the site is published to its webflow.io subdomain. Default: false (manual publishing required). If publishing fails, the operation fails with an error (the site may exist but Pulumi will report failure).
     */
    declare public readonly publish: pulumi.Output<boolean | undefined>;
    /**
     * When `publish` is true, the list of custom domain IDs (not host names) to publish to. Maps to the `customDomains` field of the Webflow publish endpoint. Custom domain IDs can be read from the Webflow site settings or the sites API.
     */
    declare public readonly publishCustomDomains: pulumi.Output<string[] | undefined>;
    /**
     * When `publish` is true, publish only the page with this ID (24-character lowercase hexadecimal string) instead of the whole site. Maps to the `pageId` field of the Webflow publish endpoint.
     */
    declare public readonly publishPageId: pulumi.Output<string | undefined>;
    /**
     * Scope of the last publish triggered by this provider (read-only): 'site' when the whole site was published, 'page' when only `publishPageId` was published. Empty when the provider has not published the site.
     */
    declare public /*out*/ readonly publishScope: pulumi.Output<string | undefined>;
    /**
     * When `publish` is true, publish to the site's default webflow.io subdomain. Maps to the `publishToWebflowSubdomain` field of the Webflow publish endpoint. Default: false.
     */
    declare public readonly publishToWebflowSubdomain: pulumi.Output<boolean | undefined>;
    /**
     * The slugified version of the site name used in URLs (read-only). Auto-generated by Webflow from the displayName. Cannot be set via API. Examples: 'my-site', 'company-blog-2024', 'product-landing'.
     */
    declare public /*out*/ readonly shortName: pulumi.Output<string | undefined>;
    /**
     * The template to use for site creation. Optional - if not specified, Webflow will create a blank site. **This value only affects creation.** The Webflow API does not report which template a site was created from, so it cannot be read back: after `pulumi import` the state holds no templateName, and adding one to the program later does not change or replace the site. **WARNING:** changing from one non-empty template to a different non-empty template triggers a REPLACE operation, which will: (1) DELETE your existing site and ALL its content (pages, CMS items, assets, etc.), (2) CREATE a new site with the new template, (3) REPLACE all dependent resources (redirects, robots.txt, etc.). This is a DESTRUCTIVE operation that cannot be undone. Use any valid Webflow template identifier (e.g., 'mast-framework', 'blank'). Consider using resource protection (`protect: true`) to prevent accidental replacement.
     */
    declare public readonly templateName: pulumi.Output<string | undefined>;
    /**
     * The IANA timezone identifier for the site (read-only). This value is configured in Webflow Site Settings and cannot be changed via API. Examples: 'America/New_York', 'Europe/London', 'Asia/Tokyo', 'UTC'.
     */
    declare public /*out*/ readonly timeZone: pulumi.Output<string | undefined>;
    /**
     * The Webflow workspace ID where the site will be created (24-character lowercase hexadecimal string). Required for site creation (Enterprise workspace and the `workspace:write` scope are required by the Webflow API). Example: '5f0c8c9e1c9d440000e8d8c3'. You can find your workspace ID in the Webflow dashboard under Account Settings > Workspace. Changing this value replaces the site.
     */
    declare public readonly workspaceId: pulumi.Output<string>;

    /**
     * Create a Site resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SiteArgs, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (!opts.id) {
            if (args?.displayName === undefined && !opts.urn) {
                throw new Error("Missing required property 'displayName'");
            }
            if (args?.workspaceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'workspaceId'");
            }
            resourceInputs["displayName"] = args?.displayName;
            resourceInputs["parentFolderId"] = args?.parentFolderId;
            resourceInputs["publish"] = args?.publish;
            resourceInputs["publishCustomDomains"] = args?.publishCustomDomains;
            resourceInputs["publishPageId"] = args?.publishPageId;
            resourceInputs["publishToWebflowSubdomain"] = args?.publishToWebflowSubdomain;
            resourceInputs["templateName"] = args?.templateName;
            resourceInputs["workspaceId"] = args?.workspaceId;
            resourceInputs["customDomains"] = undefined /*out*/;
            resourceInputs["dataCollectionEnabled"] = undefined /*out*/;
            resourceInputs["dataCollectionType"] = undefined /*out*/;
            resourceInputs["lastPublished"] = undefined /*out*/;
            resourceInputs["lastUpdated"] = undefined /*out*/;
            resourceInputs["previewUrl"] = undefined /*out*/;
            resourceInputs["publishScope"] = undefined /*out*/;
            resourceInputs["shortName"] = undefined /*out*/;
            resourceInputs["timeZone"] = undefined /*out*/;
        } else {
            resourceInputs["customDomains"] = undefined /*out*/;
            resourceInputs["dataCollectionEnabled"] = undefined /*out*/;
            resourceInputs["dataCollectionType"] = undefined /*out*/;
            resourceInputs["displayName"] = undefined /*out*/;
            resourceInputs["lastPublished"] = undefined /*out*/;
            resourceInputs["lastUpdated"] = undefined /*out*/;
            resourceInputs["parentFolderId"] = undefined /*out*/;
            resourceInputs["previewUrl"] = undefined /*out*/;
            resourceInputs["publish"] = undefined /*out*/;
            resourceInputs["publishCustomDomains"] = undefined /*out*/;
            resourceInputs["publishPageId"] = undefined /*out*/;
            resourceInputs["publishScope"] = undefined /*out*/;
            resourceInputs["publishToWebflowSubdomain"] = undefined /*out*/;
            resourceInputs["shortName"] = undefined /*out*/;
            resourceInputs["templateName"] = undefined /*out*/;
            resourceInputs["timeZone"] = undefined /*out*/;
            resourceInputs["workspaceId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Site.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * The set of arguments for constructing a Site resource.
 */
export interface SiteArgs {
    /**
     * The human-readable name of the site as shown in the Webflow dashboard. Required - must be a non-empty string. Examples: 'My Marketing Site', 'Company Blog', 'Product Landing Page'. This is the name users will see when managing the site.
     */
    displayName: pulumi.Input<string>;
    /**
     * The folder ID where the site will be organized in the Webflow dashboard (24-character lowercase hexadecimal string). Optional - the site will be placed at the workspace root if not specified. Removing this property from your program moves the site back to the workspace root. This is useful for organizing multiple sites into logical groups within your workspace.
     */
    parentFolderId?: pulumi.Input<string | undefined>;
    /**
     * Automatically publish the site after creation or updates. When set to true, the provider calls the Webflow publish endpoint after successfully creating or updating the site, using `publishToWebflowSubdomain`, `publishCustomDomains` and `publishPageId` to build the publish request. If neither `publishToWebflowSubdomain` nor `publishCustomDomains` is set, the site is published to its webflow.io subdomain. Default: false (manual publishing required). If publishing fails, the operation fails with an error (the site may exist but Pulumi will report failure).
     */
    publish?: pulumi.Input<boolean | undefined>;
    /**
     * When `publish` is true, the list of custom domain IDs (not host names) to publish to. Maps to the `customDomains` field of the Webflow publish endpoint. Custom domain IDs can be read from the Webflow site settings or the sites API.
     */
    publishCustomDomains?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * When `publish` is true, publish only the page with this ID (24-character lowercase hexadecimal string) instead of the whole site. Maps to the `pageId` field of the Webflow publish endpoint.
     */
    publishPageId?: pulumi.Input<string | undefined>;
    /**
     * When `publish` is true, publish to the site's default webflow.io subdomain. Maps to the `publishToWebflowSubdomain` field of the Webflow publish endpoint. Default: false.
     */
    publishToWebflowSubdomain?: pulumi.Input<boolean | undefined>;
    /**
     * The template to use for site creation. Optional - if not specified, Webflow will create a blank site. **This value only affects creation.** The Webflow API does not report which template a site was created from, so it cannot be read back: after `pulumi import` the state holds no templateName, and adding one to the program later does not change or replace the site. **WARNING:** changing from one non-empty template to a different non-empty template triggers a REPLACE operation, which will: (1) DELETE your existing site and ALL its content (pages, CMS items, assets, etc.), (2) CREATE a new site with the new template, (3) REPLACE all dependent resources (redirects, robots.txt, etc.). This is a DESTRUCTIVE operation that cannot be undone. Use any valid Webflow template identifier (e.g., 'mast-framework', 'blank'). Consider using resource protection (`protect: true`) to prevent accidental replacement.
     */
    templateName?: pulumi.Input<string | undefined>;
    /**
     * The Webflow workspace ID where the site will be created (24-character lowercase hexadecimal string). Required for site creation (Enterprise workspace and the `workspace:write` scope are required by the Webflow API). Example: '5f0c8c9e1c9d440000e8d8c3'. You can find your workspace ID in the Webflow dashboard under Account Settings > Workspace. Changing this value replaces the site.
     */
    workspaceId: pulumi.Input<string>;
}
