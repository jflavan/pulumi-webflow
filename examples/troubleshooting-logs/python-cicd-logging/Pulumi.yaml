name: webflow-logging-cicd
runtime: python
description: CI/CD logging configuration example for production-grade deployments

config:
  webflow:apiToken:
    description: Webflow API token (or set WEBFLOW_API_TOKEN)
    secret: true
  workspaceId:
    description: Webflow workspace ID that owns the sites (Enterprise workspace required for site creation)
