name: webflow-multi-environment-python
runtime: python
description: Multi-environment Webflow site deployment with stack-specific configuration (Python)

config:
  webflow:
    description: Webflow provider configuration
  workspaceId:
    description: Webflow workspace ID that owns the sites (Enterprise workspace required for site creation)
